import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
    images: {
    domains: ['i.pinimg.com', 'naamii.org.com.np', 'www.naamii.org.np', 'naamii.com.np','images.unsplash.com'],
  },
};

export default nextConfig;
